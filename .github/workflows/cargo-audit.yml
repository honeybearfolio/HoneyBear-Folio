name: Cargo Audit

on:
  pull_request:
    paths:
      - 'app/src-tauri/**'
  push:
    branches:
      - main
    paths:
      - 'app/src-tauri/**'
  schedule:
    - cron: '0 6 * * 1'

jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
      issues: write

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: app/src-tauri

      - name: Run cargo audit
        uses: actions-rust-lang/audit@v1
        with:
          workingDirectory: app/src-tauri
